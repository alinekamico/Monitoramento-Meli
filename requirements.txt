requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
flask>=3.0.0
flask-cors>=4.0.0

# MVP Buybox — persistência local em SQLite via ORM.
# Escolhemos SQLAlchemy 2.x para abstrair queries e permitir
# migração futura para Postgres sem reescrever a camada.
sqlalchemy>=2.0.0

# Testes da lógica de pricing, regras de alerta e persistência.
pytest>=8.0.0
